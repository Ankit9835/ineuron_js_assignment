// 04. Declare a varibale and assign string value to it and check if the string contains a word Script using includes() method.

const text = 'Namaste Javascript'
const result = text.includes('Javascript')
if (result) {
  console.log('word included')
} else {
  console.log('word not included')
}
